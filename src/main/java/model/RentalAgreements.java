package model;

import java.util.ArrayList;
import java.util.HashMap;

public class RentalAgreements {
	private ArrayList<RentalAgreement> rentalAgreementsList;

	// Constructor
	public RentalAgreements() {
		rentalAgreementsList = new ArrayList<RentalAgreement>();
	}

	/**
	 * Create a new rental agreement
	 * @param result - a hashmap containing the rental agreement details
	 * @return true if the rental agreement is created successfully otherwise false
	 */
	public boolean createRentalAgreementFromForm(HashMap<String, String> result) {
		// TODO: Implement this
		// destruct the hashmap

		// create a new rental agreement

		// add the rental agreement to the list

		// return true if the rental agreement is created successfully

		// display error/handling exception

		return false;
	}
}
